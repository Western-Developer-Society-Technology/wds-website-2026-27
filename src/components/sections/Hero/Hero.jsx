import SectionStub from "@/components/ui/SectionStub";

// Prompt 1 replaces this. Design: node 128:396, y 0–1115, ink background.
export default function Hero() {
  return <SectionStub id="hero" label="hero" tone="dark" height={1115} />;
}
